version: "3.9"
services:
  predator12:
    build: ..
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    environment:
      UVICORN_LOG_LEVEL: info
    restart: unless-stopped
